# Code of Conduct

The project lives in [`dotnet/`](dotnet/) — see
[`dotnet/CODE_OF_CONDUCT.md`](dotnet/CODE_OF_CONDUCT.md) (Contributor
Covenant). Kept here too so GitHub's automatic community-health
detection picks it up (it only scans the repo root, `.github/`, or
`docs/`, not arbitrary subdirectories).
