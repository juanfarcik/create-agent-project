# Changelog

The project lives in [`dotnet/`](dotnet/) — see
[`dotnet/CHANGELOG.md`](dotnet/CHANGELOG.md).

## History

The project started as a Python prototype (originally documented in this
file) and was fully ported to C#/.NET; the Python implementation was
removed once the port reached parity.
