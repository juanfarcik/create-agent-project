architecture:
  profile: autonomous-loop
  loop_pattern: auto
  memory: filesystem
  checkpoints: true
  complexity: MEDIUM
  estimated:
    calls_per_run: 4-8/run
    context: LOW
    cost: LOW-MEDIUM
  agents:
    - role: orchestrator
    mode: always
    model_tier: balanced
    - role: researcher
    mode: on-demand
    model_tier: cheap
    - role: evaluator
    mode: always
    model_tier: balanced
  human_gates:
    - budget threshold
    - irreversible actions
  notes:
    - Persistent state + scheduled/continuous execution with checkpoints.
