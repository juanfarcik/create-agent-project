# Security Policy

The project lives in [`dotnet/`](dotnet/) — see
[`dotnet/SECURITY.md`](dotnet/SECURITY.md) for how to report a
vulnerability. Kept here too so GitHub's Security tab picks it up
automatically (root-only detection).
